const csvUrl="https://docs.google.com/spreadsheets/d/e/2PACX-1vRr62BlKCzICpC0ctnU2mRB8cq_SOCcsgydXQJXD5pQvasO1b1iT0Wp_L7sFxH8UGJCepaMjng1GUO0/pub?gid=1610793698&single=true&output=csv";const cotizacionUrl="https://docs.google.com/spreadsheets/d/e/2PACX-1vQJ7PS8pvnCKdpLgJFonsZNN54Rs8oTpzGgCxhbfZzd3KmKb9k12OEwgAWuDAHiIPraWKxoS5TlCm4X/pub?gid=0&single=true&output=csv";fetch(cotizacionUrl).then((res)=>res.text()).then((csv)=>{const lineas=csv.trim().split("\n");if(lineas.length>0){const valorDolar=lineas[0].split(",")[0].replace(",",".");document.getElementById("dolarValor").textContent=valorDolar||"N/A"}else{document.getElementById("dolarValor").textContent="Sin datos"}}).catch((err)=>{document.getElementById("dolarValor").textContent="Error";console.error("Error al obtener dólar:",err)});const fechaFormateada=new Intl.DateTimeFormat("es-AR").format(new Date());document.getElementById("fechaActual").textContent=fechaFormateada;Papa.parse(csvUrl,{download:!0,skipEmptyLines:!0,complete:function(results){const rows=results.data;const categorias=[];let categoriaActual=null;let esperandoHeaders=!1;rows.forEach((row)=>{const isCategoria=row[0]&&row.slice(1).every((cell)=>!cell.trim());if(isCategoria){categoriaActual={nombre:row[0],headers:[],productos:[]};categorias.push(categoriaActual);esperandoHeaders=!0}else if(esperandoHeaders&&categoriaActual){categoriaActual.headers=row;esperandoHeaders=!1}else if(categoriaActual&&row.length>0){while(row.length<categoriaActual.headers.length)row.push("");categoriaActual.productos.push(row)}});renderCategorias(categorias)},error:function(err){console.error("Error al parsear CSV:",err)}});const iconosCategorias={IPHONE:"bi-apple",AIRPODS:"bi-earbuds","APPLE WATCH":"bi-smartwatch",AIRTAG:"bi-tag",IPAD:"bi-tablet-fill","MACBOOK AIR":"bi-laptop","MACBOOK PRO":"bi-laptop","SONY PLAYSTATION":"bi-playstation","MICROSOFT XBOX":"bi-xbox","NINTENDO SWITCH":"bi-nintendo-switch",VR:"bi-headset-vr",GOPRO:"bi-camera-video",INSTA360:"bi-camera-video"};const badges={NEW:`<span class="badge bg-warning text-dark">✈️¡NUEVO!</span>`,SALE:`<span class="badge bg-danger text-white">🔥¡OFERTA!</span>`,HOT:`<span class="badge bg-primary">🔥 HOT</span>`,LIMITED:`<span class="badge bg-info text-dark">⏳ Edición limitada</span>`};function renderCategorias(categorias){const container=document.getElementById("categoriasContainer");container.innerHTML="";categorias.forEach((cat,index)=>{const catId=`cat-${index}`;const ocultarColumnaIndex=cat.headers.indexOf("Ocultar");const icono=iconosCategorias[cat.nombre]||"bi bi-phone";const card=document.createElement("div");card.className="accordion mb-1";card.innerHTML=`
      <div class="accordion-flush">
        <h2 class="accordion-header" id="heading-${catId}">
          <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-${catId}" aria-expanded="false" aria-controls="collapse-${catId}">
            <i class="${icono} me-2"></i> ${cat.nombre}
          </button>
        </h2>
        <div id="collapse-${catId}" class="accordion-collapse collapse" aria-labelledby="heading-${catId}">
          <div class="accordion-body">
  ${renderAlerta(cat.nombre)}
            <div class="table-responsive">
              <table class="table table-bordered table-sm text-center" id="tabla-${catId}">
                <thead class="table-light">
                  <tr>
                    ${cat.headers
                        .filter((h) => h !== "Ocultar")
                        .map((h) => `<th>${h}</th>`)
                        .join("")}
                  </tr>
                </thead>
                <tbody>
                  ${cat.productos
                      .filter((row) => ocultarColumnaIndex === -1 || row[ocultarColumnaIndex].toLowerCase() !== "x")
                      .map((row) => {
                          const esFilaVacia = row.every((cell) => cell === "");
                          if (esFilaVacia) {
                              return `<tr><td colspan="${cat.headers.length - (ocultarColumnaIndex !== -1 ? 1 : 0)}">&nbsp;</td></tr>`;
                          }

                          return `<tr>${row.map((cell,index)=>{if(index===ocultarColumnaIndex)return null;if(cat.headers[index]==="L"&&cell){const badge=badges[cell];return `<td>${badge || cell}</td>`}
const primeraColumnaVisible=row.findIndex((_,i)=>i!==ocultarColumnaIndex)===index;return `<td>${primeraColumnaVisible ? `<strong>${cell}</strong>` : cell || "&nbsp;"}</td>`}).filter((cell)=>cell!==null).join("")}</tr>`;
                      })
                      .join("")}
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    `;container.appendChild(card)})}
function renderAlerta(nombreCategoria){const mensajes={"MACBOOK AIR":"Consultanos por versiones custom (Chip MAX, +RAM, etc). También traemos iMac, Mac mini, Mac Studio","MACBOOK PRO":"Consultanos por versiones custom (Chip MAX, +RAM, etc). También traemos iMac, Mac mini, Mac Studio",IPHONE:"¡Combo cargador original (Apple 20W, MWVV3AM) + U$30!",SAMSUNG:"Por otros productos de la marca (Linea Z, Samsung Tab, etc) consultanos por WhatsApp"};if(!mensajes[nombreCategoria])return"";return `
    <div class="alert alert-warning d-flex align-items-center mb-3 py-2 px-3" role="alert">
      <i class="bi bi-info-circle-fill me-2"></i>
      <div class="small">${mensajes[nombreCategoria]}</div>
    </div>
  `}
const filtrarGeneral=()=>{const term=document.getElementById("searchGeneral").value.toLowerCase().trim();const words=term?term.split(/\s+/):[];const resultEl=document.getElementById("resultadoBusqueda");let matchCount=0;document.querySelectorAll(".accordion-flush").forEach((item)=>{const btn=item.querySelector(".accordion-button");const collapseEl=item.querySelector(".collapse");const bs=bootstrap.Collapse.getOrCreateInstance(collapseEl,{toggle:!1});if(!term){item.style.display="";bs.hide();btn.classList.add("collapsed");btn.setAttribute("aria-expanded","false");item.querySelectorAll("tbody tr").forEach((row)=>{row.style.display=""});return}
const headerText=btn.innerText.toLowerCase();const rows=Array.from(item.querySelectorAll("tbody tr"));let itemHasMatch=!1;rows.forEach((row)=>{const raw=Array.from(row.cells).filter((_,i)=>i!==2).map((td)=>td.innerText).join(" ").toLowerCase()+" "+headerText;if(!row.innerText.trim()){row.style.display="none"}else{const isMatch=words.every((w)=>raw.includes(w));row.style.display=isMatch?"":"none";if(isMatch){itemHasMatch=!0;matchCount++}}});if(itemHasMatch){item.style.display="";bs.show();btn.classList.remove("collapsed");btn.setAttribute("aria-expanded","true")}else{bs.hide();btn.classList.add("collapsed");btn.setAttribute("aria-expanded","false");item.style.display="none"}});if(!term){resultEl.textContent="";resultEl.classList.remove("no-resultado","resultado-encontrado")}else if(matchCount===0){resultEl.textContent="Sin resultados. Consultar existencia por WhatsApp";resultEl.classList.add("no-resultado");resultEl.classList.remove("resultado-encontrado")}else{resultEl.textContent=`${matchCount} resultado${matchCount !== 1 ? "s" : ""} encontrado${matchCount !== 1 ? "s" : ""}`;resultEl.classList.add("resultado-encontrado");resultEl.classList.remove("no-resultado")}};function debounce(fn,wait){let timeout;return(...args)=>{clearTimeout(timeout);timeout=setTimeout(()=>fn.apply(this,args),wait)}}
document.getElementById("searchGeneral").addEventListener("input",debounce(filtrarGeneral,300));document.getElementById("clearSearch").addEventListener("click",function(){const searchInput=document.getElementById("searchGeneral");searchInput.value="";searchInput.dispatchEvent(new Event("input"))});function buscarMarca(nombreMarca){const searchInput=document.getElementById("searchGeneral");searchInput.value=nombreMarca;searchInput.dispatchEvent(new Event("input"))}
document.getElementById("faqModal").addEventListener("hidden.bs.modal",()=>{const accordions=document.querySelectorAll("#faqModal .accordion-collapse.show");accordions.forEach((ac)=>{new bootstrap.Collapse(ac,{toggle:!1}).hide()})})
